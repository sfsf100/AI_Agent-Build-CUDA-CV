# SAD Stereo Matching Skill

## Metadata
- **Skill Name**: SAD_StereoMatching
- **Version**: 1.0.0
- **Category**: Computer Vision > Stereo Vision > Dense Matching
- **Author**: shuyo
- **Date**: 2026-02-27
- **Difficulty**: Intermediate
- **Tags**: #stereo-vision #depth-estimation #block-matching #real-time #opencv

---

## Purpose

### Problem Statement

在立體視覺系統中，從兩個不同視角拍攝的影像對（stereo pair）計算深度資訊是核心任務。

**白話文**：想像你閉上左眼和右眼交替看一個物體，近的東西「跳動」很大，遠的東西幾乎不動。立體匹配就是用數學計算這個「跳動量」（視差），從而推算出距離。

傳統 CPU 實現需要大量的窗口比對運算，速度太慢，無法即時使用。本 Skill 使用 CUDA 在 GPU 上平行計算，大幅加速。

### Use Cases

| 應用領域 | 具體場景 |
|---------|---------|
| 自動駕駛 | 即時障礙物距離估計、路徑規劃 |
| 機器人視覺 | 立體避障、物體抓取的深度感知 |
| AR / VR | 場景深度重建、虛實融合 |
| 無人機 | 地形測繪、自主導航 |
| 工業檢測 | 3D 表面檢測、尺寸測量 |
| 醫療影像 | 立體內視鏡的深度資訊 |

### Background

**核心原理**：

```
左相機看到物體在 x=100 的位置
右相機看到同一物體在 x=85 的位置
 → 視差 d = 100 - 85 = 15 像素
 → 深度 Z = (焦距 × 基線) / 15
```

**SAD 演算法是什麼？**

SAD = Sum of Absolute Differences（絕對差值總和）。

它的做法很直覺：
1. 在左圖取一塊小窗口（例如 7×7 像素）
2. 在右圖的同一行上滑動這個窗口，每個位置算「差異有多大」
3. 差異最小的位置，就是最佳匹配 → 偏移量就是視差

| 特性 | 說明 |
|------|------|
| ✅ 優點 | 計算簡單、非常適合 GPU 平行加速、結果穩定 |
| ❌ 缺點 | 對無紋理區域（如白牆）容易出錯 |
| 🎯 適用 | 速度要求高、場景紋理豐富的應用 |

---

## Interface

### Function Signature

```
函數名稱: SAD_GPU

輸入:
  leftImage   ← 左相機影像（灰階）
  rightImage  ← 右相機影像（灰階，尺寸必須和左圖相同）

輸出:
  disparityMap ← 視差圖（每個像素的值 = 該點的視差）

可調參數:
  kernelSize  ← 匹配窗口大小（預設 7）
  searchRange ← 最大搜尋範圍（預設 60）
```

### Inputs

| Name | Type | Shape | Constraints | Description |
|------|------|-------|-------------|-------------|
| leftImage | 灰階影像 | H × W | 單通道、8-bit | 左側相機拍攝的影像 |
| rightImage | 灰階影像 | H × W | 必須與 leftImage 尺寸相同 | 右側相機拍攝的影像 |

**注意**：
- 輸入影像必須已經完成**校正**（rectified），即對應點位在同一水平線上
- 如果是彩色影像，需先轉為灰階
- 典型尺寸：640×480（VGA）到 1920×1080（Full HD）

### Outputs

| Name | Type | Shape | Description |
|------|------|-------|-------------|
| disparityMap | 灰階影像 | H × W, 8-bit | 視差圖。像素值 = 視差（0 ~ searchRange） |

**如何解讀輸出？**

```
視差圖中：
  亮的地方 → 視差大 → 距離近
  暗的地方 → 視差小 → 距離遠
  黑色 (0) → 無效區域（邊界或匹配失敗）

要換算成真實深度：
  深度 Z = (焦距 × 基線) / 視差值
```

### Parameters

| Name | Type | Default | Valid Range | Description |
|------|------|---------|-------------|-------------|
| kernelSize | 整數 | 7 | 3, 5, 7, 9, 11, 13, 15（必須是奇數） | 匹配窗口的邊長 |
| searchRange | 整數 | 60 | 1 ~ 128 | 在右圖上搜尋的最大偏移像素數 |

**參數怎麼選？**

| 參數 | 值小的效果 | 值大的效果 | 建議 |
|------|-----------|-----------|------|
| kernelSize | 保留細節，但對噪聲敏感 | 結果平滑，但細節模糊 | 初學者用 **7** |
| searchRange | 只能偵測遠物體 | 可偵測近物體，但計算量大 | 依場景設定，一般 **60** 夠用 |

---

## Algorithm

### High-Level Steps（概要步驟）

用一張流程圖表示：

```
Step 1              Step 2              Step 3              Step 4              Step 5
分配 GPU 記憶體 →   影像傳入 GPU →     GPU 平行匹配計算 →  結果傳回 CPU →     釋放 GPU 記憶體
(cudaMalloc)        (Host→Device)      (每個像素一個thread) (Device→Host)      (cudaFree)
```

### Detailed Logic（詳細邏輯）

#### Step 3 的細節：每個像素怎麼計算？

以影像中的一個像素 (x, y) 為例：

```
第一步：邊界檢查
  └─ 這個像素離邊界夠遠嗎？（至少要 kernelSize/2 個像素的距離）
     ├─ 不夠遠 → 直接設為 0（無效），跳過
     └─ 夠遠 → 進入第二步

第二步：逐一嘗試每個視差值
  └─ 視差 d 從 0 嘗試到 searchRange-1：
     │
     ├─ 在左圖以 (x, y) 為中心，取出 kernelSize × kernelSize 的窗口
     ├─ 在右圖以 (x+d, y) 為中心，取出同樣大小的窗口
     ├─ 兩個窗口逐像素相減，取絕對值，全部加總 → 得到 SAD 值
     │
     └─ 如果這個 SAD 值是目前最小的 → 記住這個 d

第三步：輸出
  └─ disparityMap[y][x] = 產生最小 SAD 的那個 d 值
```

#### 圖解範例

```
左圖窗口 (3×3)         右圖窗口 (d=5)          絕對差
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│ 120  130  125│       │ 118  132  120│       │  2    2    5 │
│ 135  140  138│  -    │ 130  142  135│  =    │  5    2    3 │
│ 128  132  130│       │ 125  130  128│       │  3    2    2 │
└─────────────┘       └─────────────┘       └─────────────┘

SAD = 2+2+5+5+2+3+3+2+2 = 26
```

對所有 d 值（0 到 searchRange-1）都算一次 SAD，取最小的。

### Mathematical Formula（數學公式）

```
                    k      k
SAD(x, y, d) =    Σ      Σ     |左圖(x+i, y+j) - 右圖(x+d+i, y+j)|
                  i=-k   j=-k

其中：
  k = (kernelSize - 1) / 2     ← 窗口半徑
  d = 候選視差 (0, 1, 2, ..., searchRange-1)

最終結果：
  disparity(x, y) = 使 SAD(x, y, d) 最小的那個 d 值
```

### Pseudocode（虛擬碼）

```
函數 SAD_GPU(左圖, 右圖, 視差圖, kernelSize, searchRange):
    
    // ─── GPU 上平行處理，每個像素獨立計算 ───
    
    對每個像素 (x, y)【在 GPU 上同時執行】:
        
        half = (kernelSize - 1) / 2
        
        // 邊界檢查
        如果 x < half 或 x >= 寬度-half 或 y < half 或 y >= 高度-half:
            視差圖[y][x] = 0
            結束此像素
        
        // 搜尋最佳視差
        最小SAD = 無限大
        最佳視差 = 0
        
        對每個候選視差 d = 0 到 searchRange-1:
            
            目前SAD = 0
            
            // 計算窗口內的 SAD
            對窗口內每個偏移 (dy, dx):    // dy, dx 範圍為 -half 到 +half
                左邊像素值 = 左圖[y + dy][x + dx]
                右邊像素值 = 右圖[y + dy][x + d + dx]
                目前SAD = 目前SAD + |左邊像素值 - 右邊像素值|
            
            // 更新最小值
            如果 目前SAD < 最小SAD:
                最小SAD = 目前SAD
                最佳視差 = d
        
        // 寫入結果
        視差圖[y][x] = 最佳視差
```

---

## Performance Requirements

### Target Hardware

| 等級 | 硬體 | 架構 | CUDA Cores | 記憶體 |
|------|------|------|------------|--------|
| 主要目標 | NVIDIA RTX 3060 | Ampere (SM 8.6) | 3,584 | 12 GB GDDR6 |
| 次要目標 | Jetson Xavier NX | Volta (SM 7.2) | 384 | 8 GB shared |
| 最低要求 | Compute Capability ≥ 3.5 | Kepler 以上 | — | ≥ 2 GB |

### Performance Targets

| 指標 | 目標值 | 測量條件 |
|------|-------|---------|
| 處理延遲 | < 10 ms | 1280×720, RTX 3060, kernel=7, range=60 |
| 吞吐率 | > 100 FPS | 640×480, RTX 3060 |
| vs CPU 加速比 | > 20x | 對比 OpenCV CPU StereoBM |
| 記憶體使用 | < 200 MB | 1920×1080 輸入 |

**不同解析度的目標**：

| 解析度 | RTX 3060 目標 FPS | Jetson Xavier NX 目標 FPS |
|--------|-------------------|--------------------------|
| 640×480 | 200+ | 60+ |
| 1280×720 | 100+ | 30+ |
| 1920×1080 | 50+ | 15+ |

---

## Optimization Strategy

### 瓶頸分析（為什麼原始版本會慢）

**最大問題：GPU 記憶體存取太頻繁**

```
每個像素需要讀取記憶體的次數：
  searchRange × kernelSize² × 2（左右圖）
= 60 × 49 × 2
= 5,880 次！

而且很多相鄰像素會讀取「重疊的」資料 → 白白浪費頻寬
```

### 優化優先級

#### 🔴 Priority 1：Shared Memory Tiling（預期加速 3～5 倍）

**白話文**：把資料先搬到 GPU 的高速暫存區（shared memory），大家共用，不用每次都去慢速記憶體讀取。

```
比喻：
  沒優化 = 每次做菜都跑去超市買食材
  有優化 = 先把食材全搬到廚房台面上，做菜時直接拿
```

#### 🟠 Priority 2：Texture Memory（預期額外加速 20～30%）

**白話文**：利用 GPU 內建的「影像專用快取」，自動幫你處理邊界和快取。

#### 🟡 Priority 3：Warp-level Reduction（預期額外加速 10～15%）

**白話文**：讓 GPU 內「同一組」的 32 個計算單元，直接互相傳資料，不用繞道記憶體。

#### 🟢 Priority 4：記憶體合併存取

**白話文**：讓相鄰的計算單元讀取相鄰的記憶體位置，GPU 可以「一次打包讀取」，效率最高。

### Parallelization Strategy（平行化策略）

```
Thread 分配方式：
  1 個 GPU thread = 1 個輸出像素

Block 大小建議：
  32 × 32 = 1,024 threads（剛好對齊 GPU 的 warp 大小）

Grid 大小：
  (影像寬度 ÷ 32) × (影像高度 ÷ 32)

範例（1280×720 影像）：
  Grid = (40, 23) 個 blocks
  總共 ≈ 940,000 個 threads 同時運算
```

---

## Edge Cases & Constraints

### Input Validation（輸入驗證清單）

| # | 檢查項目 | 不通過時怎麼辦 |
|---|---------|---------------|
| 1 | 左右圖尺寸是否相同 | 報錯：「Image size mismatch」 |
| 2 | 是否為灰階影像 | 自動轉換，或報錯要求使用者轉換 |
| 3 | 影像是否為空 | 報錯：「Empty input image」 |
| 4 | kernelSize 是否為 3~15 的奇數 | 報錯並提示有效範圍 |
| 5 | searchRange 是否在 1~128 內 | 報錯並提示有效範圍 |
| 6 | GPU 記憶體是否足夠 | 建議降低解析度或釋放資源 |

### Boundary Handling（影像邊界處理）

影像邊緣的像素無法提取完整窗口，有三種策略：

| 策略 | 做法 | 優點 | 缺點 | 建議 |
|------|------|------|------|------|
| **Skip Border** | 邊界像素直接設 0 | 簡單快速 | 邊緣沒有視差 | ✅ 推薦（初學者用） |
| Zero Padding | 影像外視為黑色 | 保留邊界 | 可能誤匹配 | 進階使用 |
| Replicate Padding | 複製最近像素 | 較自然 | 額外計算 | 進階使用 |

### Special Cases（特殊情況）

| 情況 | 問題描述 | 處理方式 |
|------|---------|---------|
| **無紋理區域** | 白牆等區域匹配結果隨機 | 當 SAD 值相同時選最小 d；可加中值濾波後處理 |
| **遮擋區域** | 左圖看得到但右圖被遮住 | 若 minSAD 過大，標記為無效 |
| **重複紋理** | 磚牆等週期性紋理可能匹配錯 | 加大 kernelSize 或換用 NCC 等進階算法 |
| **超出搜尋範圍** | 真實視差 > searchRange | 根據場景增大 searchRange |

### Error Handling（錯誤處理原則）

| 錯誤類型 | 處理方式 |
|---------|---------|
| CUDA 記憶體分配失敗 | 提示降低影像解析度 |
| Kernel 啟動失敗 | 檢查 GPU 是否可用、參數是否溢位 |
| 結果異常（有效視差 < 50%） | 輸出警告，建議檢查輸入影像或參數 |

---

## Testing & Validation

### Unit Tests（單元測試）

#### Test 1：簡單水平位移

- **目的**：驗證基本正確性
- **輸入**：左圖為垂直條紋，右圖為左圖向右位移 15 像素
- **預期**：視差圖所有有效像素 ≈ 15（容差 ±1）
- **通過標準**：正確率 > 95%

#### Test 2：標準測試影像（Tsukuba）

- **目的**：與已知正確答案（ground truth）比較
- **輸入**：Tsukuba 立體影像對（384×288，視差範圍 0–15）
- **指標**：Bad Pixel Percentage = 誤差 > 1 pixel 的像素佔比
- **通過標準**：Bad Pixel < 10%

#### Test 3：邊界條件

- **目的**：確保小影像、邊界像素不會造成程式崩潰
- **輸入**：20×20 的均勻灰階影像
- **預期**：邊界像素（距邊 < kernel/2）全為 0，程式不崩潰

#### Test 4：不同參數組合

- **目的**：確保所有有效參數組合都能正常運作
- **測試矩陣**：

| 解析度 | kernelSize | searchRange |
|--------|-----------|-------------|
| 640×480 | 5 | 32 |
| 1280×720 | 7 | 64 |
| 1920×1080 | 9 | 96 |
| 320×240 | 11 | 16 |

### Performance Benchmark（效能測試）

| 測試項目 | 方法 |
|---------|------|
| GPU 加速比 | 同一輸入，比較 GPU 版本 vs OpenCV CPU StereoBM 的耗時 |
| 不同解析度 FPS | 連續處理 100 張影像，計算平均 FPS |
| 記憶體用量 | 用 `nvidia-smi` 監控峰值記憶體 |

---

## 版本記錄

| 版本 | 日期 | 變更說明 |
|------|------|---------|
| 1.0.0 | 2026-02-27 | 初版：完整 SAD 立體匹配規格書 |